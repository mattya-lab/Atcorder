#include<bitc/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;
typedef Long Long ll;

int main(){
  int a, b, c, d;
  cin >> a >> b >> c >> d;
  if(a != c) puts("1"); else puts("0");
  return 0;
}